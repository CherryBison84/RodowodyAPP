[project]
name = "zubry-pedigree-app"
version = "0.1.0"
description = "Aplikacja do analizy rodowodowej żubrów (Tkinter + Streamlit)"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]

